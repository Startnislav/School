#include <stdio.h>

int main( int number=0x00ccbbaa;
//printf("%d",number);

for(int i=1;i<5;i++){
	int shift=(5-i)*8;
	unsigned char totalShift;
	totalShift=number>>shift;
	unsigned char g=0x000000ff;
	unsigned char total=totalShift & g;
	printf("%d element by bit %c \n",i,total);
	}
}
